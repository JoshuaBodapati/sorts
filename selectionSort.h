#include "utility.h"

#pragma once

using namespace std;

/* repeat num of elements -1 times

    set the first unsorted element as min (map?) 
    for each unsorted
        if element < current min
            current min = elt
    swap min first unsorted.

*/

